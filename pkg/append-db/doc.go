/*
package append defines an opinionated, object-store oriented wrapper around carapace/cellar.

An object in append-db starts out by defining all possible fields through protocol buffers, and then only being
able to amend the object.
*/
package append
